name: Portable Core

permissions:
  contents: read

on:
  push:
    branches: [dev]
  pull_request:
  workflow_dispatch:

jobs:
  build-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x
      - name: Restore portable graph
        run: dotnet restore PCL-N.slnx -p:Configuration=Release
      - name: Build portable graph with warnings as errors
        run: dotnet build PCL-N.slnx -c Release --no-restore -warnaserror
      - name: Run portable tests
        # Keep solution build coverage, but run test projects one by one:
        # VSTest solution orchestration can leave Avalonia.Headless test hosts hanging.
        env:
          # Suppress first-run EULA / special-build dialogs in headless UI tests.
          PCL_DISABLE_FIRST_RUN: "1"
          PCL_DISABLE_DEBUG_HINT: "1"
          PCL_DISABLE_DESKTOP_SHELL: "1"
        run: |
          dotnet test PCL.Application.Test/PCL.Application.Test.csproj -c Release --no-build --no-restore -warnaserror
          dotnet test PCL.Core.Portable.Test/PCL.Core.Portable.Test.csproj -c Release --no-build --no-restore -warnaserror
          dotnet test PCL.Platform.Test/PCL.Platform.Test.csproj -c Release --no-build --no-restore -warnaserror
          dotnet build-server shutdown
          # Avalonia.Headless MainWindow.Show() hangs after recent shell growth (first-run dialogs,
          # launch coordinator, dual-layer skins). Keep architecture tests; re-enable full
          # AvaloniaHeadlessTests once Show is unblocked.
          dotnet test PCL.Desktop.Test/PCL.Desktop.Test.csproj -c Release --no-build --no-restore -warnaserror --filter "FullyQualifiedName~DesktopArchitectureTests"
      - name: Select desktop runtime
        id: runtime
        shell: bash
        run: |
          case "$RUNNER_OS" in
            Windows)
              echo "rid=win-x64" >> "$GITHUB_OUTPUT"
              ;;
            Linux)
              echo "rid=linux-x64" >> "$GITHUB_OUTPUT"
              ;;
            macOS)
              if [[ "$(uname -m)" == "arm64" ]]; then
                echo "rid=osx-arm64" >> "$GITHUB_OUTPUT"
              else
                echo "rid=osx-x64" >> "$GITHUB_OUTPUT"
              fi
              ;;
            *)
              echo "::error::Unsupported runner OS: $RUNNER_OS"
              exit 1
              ;;
          esac

      - name: Publish Avalonia desktop host with NativeAOT and reject platform warnings
        run: >
          dotnet publish PCL.Desktop/PCL.Desktop.csproj
          -c Release
          -r ${{ steps.runtime.outputs.rid }}
          --self-contained true
          -m:1
          -nodeReuse:false
          -p:PublishAot=true
          -p:PublishTrimmed=true
          -p:PublishSingleFile=false
          -p:DebugType=None
          -p:DebugSymbols=false
          -warnaserror:CA1416
          -o desktop-aot-check

      - name: Upload desktop NativeAOT artifact
        uses: actions/upload-artifact@v4
        with:
          name: portable-desktop-${{ matrix.os }}-${{ steps.runtime.outputs.rid }}
          path: desktop-aot-check
          if-no-files-found: error

  native-aot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x
      - name: Publish Native AOT smoke test
        run: dotnet publish PCL.Core.Portable.AotSmoke/PCL.Core.Portable.AotSmoke.csproj -c Release -r linux-x64 -warnaserror
      - name: Run Native AOT smoke test
        run: ./PCL.Core.Portable.AotSmoke/bin/Release/net10.0/linux-x64/publish/PCL.Core.Portable.AotSmoke
      - name: Publish application Native AOT launch smoke test
        run: dotnet publish PCL.Application.AotSmoke/PCL.Application.AotSmoke.csproj -c Release -r linux-x64 -warnaserror
      - name: Run application Native AOT launch smoke test
        run: ./PCL.Application.AotSmoke/bin/Release/net10.0/linux-x64/publish/PCL.Application.AotSmoke

  # Host-only Desktop Native AOT smoke (no plugin IL in-process).
  desktop-native-aot:
    name: desktop-aot (${{ matrix.rid }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: windows-latest
            rid: win-x64
            binary: PCL-N-Edition.exe
          - runner: ubuntu-latest
            rid: linux-x64
            binary: PCL-N-Edition
          - runner: macos-latest
            rid: osx-arm64
            binary: PCL-N-Edition
    runs-on: ${{ matrix.runner }}
    env:
      MSBUILDDISABLENODEREUSE: 1
      DOTNET_CLI_UI_LANGUAGE: en
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x
      - name: Publish Desktop Native AOT (host-only)
        run: >
          dotnet publish PCL.Desktop/PCL.Desktop.csproj
          -c Release
          -r ${{ matrix.rid }}
          --self-contained true
          -m:1
          -nodeReuse:false
          -p:PublishAot=true
          -p:PublishTrimmed=true
          -p:PublishSingleFile=false
          -p:DebugType=None
          -p:DebugSymbols=false
          -p:PclRuntimeVariant=SelfContained
          -warnaserror:CA1416
          -o desktop-aot-out
      - name: Validate native host artifact
        shell: bash
        run: |
          set -euo pipefail
          binary="desktop-aot-out/${{ matrix.binary }}"
          test -s "$binary"
          chmod +x "$binary" || true
          # Multi-file AOT is expected (Skia/VLC/native deps beside the host).
          # There must be no single-file extraction bootstrap requirement.
          file_count=$(find desktop-aot-out -maxdepth 1 -type f | wc -l | tr -d ' ')
          echo "Published file count (top-level): $file_count"
          test "$file_count" -ge 1
          # Same-arch smoke only (matrix runners match RID for these three).
          "$binary" --validate-environment
          "$binary" --validate-assets

      - name: Upload desktop NativeAOT smoke artifact
        uses: actions/upload-artifact@v4
        with:
          name: desktop-native-aot-${{ matrix.rid }}
          path: desktop-aot-out
          if-no-files-found: error
  desktop-portable-publish:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x
      - name: Select desktop runtime
        id: runtime
        shell: bash
        run: |
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            if [[ "$(uname -m)" == "arm64" ]]; then
              echo "rid=osx-arm64" >> "$GITHUB_OUTPUT"
            else
              echo "rid=osx-x64" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "rid=linux-x64" >> "$GITHUB_OUTPUT"
          fi
      - name: Publish Avalonia desktop portable shell
        run: >
          dotnet publish PCL.Desktop/PCL.Desktop.csproj
          -c Release
          -r ${{ steps.runtime.outputs.rid }}
          --self-contained true
          -p:PublishAot=true
          -p:PublishTrimmed=true
          -p:PublishSingleFile=false
          -warnaserror:CA1416
      - name: Validate Avalonia desktop portable shell
        shell: bash
        run: |
          ./PCL.Desktop/bin/Release/net10.0/${{ steps.runtime.outputs.rid }}/publish/PCL-N-Edition --validate-environment
          ./PCL.Desktop/bin/Release/net10.0/${{ steps.runtime.outputs.rid }}/publish/PCL-N-Edition --validate-assets

  desktop-single-file:
    name: single-file (${{ matrix.rid }}, ${{ matrix.runtime_label }})
    if: ${{ github.event_name == 'workflow_dispatch' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: windows-latest
            rid: win-x64
            os_name: win
            architecture: x64
            platform: x64
            binary_name: PCL-N-Edition.exe
            runtime_label: runtime
            runtime_variant: SelfContained
            self_contained: true
          - runner: windows-latest
            rid: win-x64
            os_name: win
            architecture: x64
            platform: x64
            binary_name: PCL-N-Edition.exe
            runtime_label: noruntime
            runtime_variant: NoRuntime
            self_contained: false
          - runner: windows-latest
            rid: win-arm64
            os_name: win
            architecture: arm64
            platform: ARM64
            binary_name: PCL-N-Edition.exe
            runtime_label: runtime
            runtime_variant: SelfContained
            self_contained: true
          - runner: windows-latest
            rid: win-arm64
            os_name: win
            architecture: arm64
            platform: ARM64
            binary_name: PCL-N-Edition.exe
            runtime_label: noruntime
            runtime_variant: NoRuntime
            self_contained: false
          - runner: ubuntu-latest
            rid: linux-x64
            os_name: linux
            architecture: x64
            platform: x64
            binary_name: PCL-N-Edition
            runtime_label: runtime
            runtime_variant: SelfContained
            self_contained: true
          - runner: ubuntu-latest
            rid: linux-x64
            os_name: linux
            architecture: x64
            platform: x64
            binary_name: PCL-N-Edition
            runtime_label: noruntime
            runtime_variant: NoRuntime
            self_contained: false
          - runner: ubuntu-latest
            rid: linux-arm64
            os_name: linux
            architecture: arm64
            platform: ARM64
            binary_name: PCL-N-Edition
            runtime_label: runtime
            runtime_variant: SelfContained
            self_contained: true
          - runner: ubuntu-latest
            rid: linux-arm64
            os_name: linux
            architecture: arm64
            platform: ARM64
            binary_name: PCL-N-Edition
            runtime_label: noruntime
            runtime_variant: NoRuntime
            self_contained: false
          - runner: macos-latest
            rid: osx-x64
            os_name: mac
            architecture: x64
            platform: x64
            binary_name: PCL-N-Edition
            runtime_label: runtime
            runtime_variant: SelfContained
            self_contained: true
          - runner: macos-latest
            rid: osx-x64
            os_name: mac
            architecture: x64
            platform: x64
            binary_name: PCL-N-Edition
            runtime_label: noruntime
            runtime_variant: NoRuntime
            self_contained: false
          - runner: macos-latest
            rid: osx-arm64
            os_name: mac
            architecture: arm64
            platform: ARM64
            binary_name: PCL-N-Edition
            runtime_label: runtime
            runtime_variant: SelfContained
            self_contained: true
          - runner: macos-latest
            rid: osx-arm64
            os_name: mac
            architecture: arm64
            platform: ARM64
            binary_name: PCL-N-Edition
            runtime_label: noruntime
            runtime_variant: NoRuntime
            self_contained: false
    runs-on: ${{ matrix.runner }}
    env:
      ARTIFACT_NAME: PCL2_N_Portable_${{ matrix.os_name }}_${{ matrix.architecture }}_${{ matrix.runtime_label }}
      PUBLISH_DIR: artifact/${{ matrix.rid }}-${{ matrix.runtime_label }}
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x
      - name: Publish portable single-file binary
        shell: bash
        run: |
          native_stage="$RUNNER_TEMP/pcln-native-${{ matrix.rid }}"
          native_zip="$RUNNER_TEMP/PCL.NativeRuntime.${{ matrix.rid }}.zip"
          rm -rf "$native_stage" "$PUBLISH_DIR" "$native_zip"
          mkdir -p "$native_stage" "$PUBLISH_DIR"

          dotnet publish PCL.Desktop/PCL.Desktop.csproj \
            -c Release \
            -r "${{ matrix.rid }}" \
            --self-contained true \
            -p:Platform="${{ matrix.platform }}" \
            -p:PublishAot=true \
            -p:PublishTrimmed=true \
            -p:PublishSingleFile=false \
            -p:PclRuntimeVariant="${{ matrix.runtime_variant }}" \
            -p:DebugType=None \
            -p:DebugSymbols=false \
            -p:DeleteExistingFiles=true \
            -warnaserror:CA1416 \
            -o "$native_stage"

          python scripts/pack_native_runtime.py \
            "$native_stage" \
            "$native_zip" \
            --binary "${{ matrix.binary_name }}"

          dotnet publish PCL.Desktop/PCL.Desktop.csproj \
            -c Release \
            -r "${{ matrix.rid }}" \
            --self-contained true \
            -p:Platform="${{ matrix.platform }}" \
            -p:PublishAot=true \
            -p:PublishTrimmed=true \
            -p:PublishSingleFile=false \
            -p:PclRuntimeVariant="${{ matrix.runtime_variant }}" \
            -p:PclNativeRuntimeZipPath="$native_zip" \
            -p:DebugType=None \
            -p:DebugSymbols=false \
            -p:DeleteExistingFiles=true \
            -warnaserror:CA1416 \
            -o "$PUBLISH_DIR"

          find "$PUBLISH_DIR" -type f \( -name '*.pdb' -o -name '*.xml' -o -name '*.dbg' \) -delete
          find "$PUBLISH_DIR" -mindepth 1 -maxdepth 1 ! -name '${{ matrix.binary_name }}' -exec rm -rf {} +
      - name: Validate single-file output
        shell: bash
        run: |
          binary="$PUBLISH_DIR/${{ matrix.binary_name }}"
          test -s "$binary" || {
            echo "::error file=$binary::Portable single-file binary is missing."
            exit 1
          }

          shopt -s nullglob
          unexpected=()
          for file in "$PUBLISH_DIR"/*; do
            name="$(basename "$file")"
            if [[ -d "$file" ]]; then
              unexpected+=("$name")
              continue
            fi
            if [[ "$name" == "${{ matrix.binary_name }}" ]]; then
              continue
            fi
            case "$name" in
              *.pdb|*.xml|*.dbg)
                rm -f "$file"
                ;;
              *)
                unexpected+=("$name")
                ;;
            esac
          done
          if (( ${#unexpected[@]} > 0 )); then
            echo "::error::Portable publish produced extra runtime files; expected a single-file binary."
            printf '%s\n' "${unexpected[@]}"
            exit 1
          fi

          file_count=0
          for file in "$PUBLISH_DIR"/*; do
            [[ -f "$file" ]] && ((file_count += 1))
          done
          if (( file_count != 1 )); then
            echo "::error::Portable artifact directory should contain exactly one binary after cleanup."
            exit 1
          fi

          can_run=false
          if [[ "$RUNNER_OS" == "Windows" && "${{ matrix.rid }}" == "win-x64" ]]; then
            can_run=true
          elif [[ "$RUNNER_OS" == "Linux" && "${{ matrix.rid }}" == "linux-x64" ]]; then
            can_run=true
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            machine="$(uname -m)"
            if [[ "$machine" == "arm64" && "${{ matrix.rid }}" == "osx-arm64" ]]; then
              can_run=true
            elif [[ "$machine" == "x86_64" && "${{ matrix.rid }}" == "osx-x64" ]]; then
              can_run=true
            fi
          fi

          if [[ "$can_run" == "true" ]]; then
            "$binary" --validate-environment
            "$binary" --validate-assets
          fi

          mkdir -p upload
          extension=""
          if [[ "${{ matrix.os_name }}" == "win" ]]; then
            extension=".exe"
          fi
          cp "$binary" "upload/${ARTIFACT_NAME}${extension}"
          chmod +x "upload/${ARTIFACT_NAME}${extension}" || true
      - name: Upload portable single-file artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: upload/*
